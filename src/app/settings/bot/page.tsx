import BotStudioClient from './BotStudioClient';

export const metadata = {
    title: 'Bot Studio | Settings',
    description: 'Configure your Discord bot integration',
};

export default function BotStudioPage() {
    return <BotStudioClient />;
}

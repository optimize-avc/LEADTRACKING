import HelpClient from './HelpClient';

export const metadata = {
    title: 'Help & Support | SalesTracker AI',
    description: 'Find answers to common questions and contact our support team',
};

export default function HelpPage() {
    return <HelpClient />;
}

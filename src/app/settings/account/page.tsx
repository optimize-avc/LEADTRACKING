import AccountPreferencesClient from './AccountPreferencesClient';

export const metadata = {
    title: 'Account Preferences | SalesTracker AI',
    description: 'Manage your profile, notifications, and data preferences',
};

export default function AccountPreferencesPage() {
    return <AccountPreferencesClient />;
}
